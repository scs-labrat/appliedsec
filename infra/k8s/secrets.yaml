apiVersion: v1
kind: Secret
metadata:
  name: aluskort-secrets
  namespace: aluskort
  labels:
    app.kubernetes.io/part-of: aluskort
type: Opaque
data:
  # Base64-encoded — replace with actual values via sealed-secrets or external-secrets
  ANTHROPIC_API_KEY: "REPLACE_WITH_BASE64_ENCODED_KEY"
  POSTGRES_DSN: "REPLACE_WITH_BASE64_ENCODED_DSN"
  NEO4J_URI: "REPLACE_WITH_BASE64_ENCODED_URI"
  NEO4J_PASSWORD: "REPLACE_WITH_BASE64_ENCODED_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_BASE64_ENCODED_PASSWORD"
